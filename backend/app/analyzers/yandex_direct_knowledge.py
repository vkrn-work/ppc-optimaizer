"""
База знаний для LLM-анализаторов (llm_analyzer.py, agent_command.py,
campaign_planner.py) — v1.7.0, пункт 4 запроса пользователя.

Зачем это здесь: до этой версии модель получала только цифры (датасет) и общие
инструкции о приоритете MQL/SQL, но не знала МЕХАНИКУ Яндекс Директ API и
устоявшуюся методологию диагностики этого конкретного проекта — из-за этого
модель могла предложить технически невозможное действие (например, менять
Bid на ЕПК) или продиагностировать проблему поверхностно (просто "трафик
упал"), не пройдя по дереву причин, которым реально пользуется директолог
(см. PPC_Audit_Playbook.md, приложенный пользователем).

Это НЕ полноценный RAG (нет векторной БД, эмбеддингов, поиска по докам) —
это осознанный компромисс для v1.7.0: две компактные текстовые секции,
дописываемые в system prompt каждого вызова LLM. Полноценная база документации
Яндекс Директ (сотни страниц) не влезет в контекст ни одного из 4 провайдеров
построчно — вместо этого здесь собраны факты, которые реально стоили этому
проекту денег или сломанных апдейтов (видно по комментариям в
llm_analyzer.py, direct_writer.py, cr_analyzer.py) плюс формализованная
методология аудита. Следующий шаг к настоящему RAG — вынести это в векторную
БД и добавить туда полный текст официальной документации API v5, когда будет
нужна более широкая база (нестандартные кампании, автотаргетинг и т.д.).
"""

# ─── Механика Yandex Direct API v5 — только то, что влияет на применимость
#     предложений (что технически МОЖНО записать через apply_suggestion) ────

YANDEX_API_MECHANICS = """
СПРАВКА ПО Yandex Direct API v5 (важно для применимости твоих предложений):

1. Деньги передаются в МИКРОРУБЛЯХ (умножь рубли на 1_000_000), кроме полей,
   которые в самом ответе уже в рублях (AvgCpc, Cost) — в датасете, который
   тебе дают, все суммы УŽЕ переведены в рубли, тебе не нужно конвертировать.

2. Bid (ставка ключевого слова) можно менять через API ТОЛЬКО для кампаний
   на стратегии MANUAL_CPC (это отражено в датасете полем bid_editable).
   Для автостратегий и ЕПК (Единая перформанс-кампания) API вернёт
   ошибку 8000 "неизвестный параметр" на попытку записать Keywords[].Bid —
   ставкой в таких кампаниях управляет алгоритм Яндекса, а не рекламодатель напрямую.
   ПРАВИЛО: если bid_editable=false — НИКОГДА не предлагай bid_raise/bid_lower.
   Вместо этого доступны: add_negatives (минус-слова на группу), pause
   (остановка ключа), ad_rewrite/ad_test (текст объявления) — эти рычаги
   работают независимо от стратегии назначения ставок.

3. NegativeKeywords (минус-слова) передаются ГОЛЫМИ словами без дефиса —
   Direct API отклоняет слово с дефисом в начале/конце ошибкой 5002.
   Метод adgroups.update ПОЛНОСТЬЮ ЗАМЕНЯЕТ список минус-слов группы — если
   предлагаешь минус-слова, перечисляй их отдельно, механизм применения сам
   дописывает их к уже существующим, тебе не нужно повторять старые.

4. keywords.add (новые ключевые фразы в существующую группу) не принимает
   Bid при создании — ставку новому ключу назначит стратегия группы/кампании
   так же, как бы если бы его добавили вручную через интерфейс. Группу для
   новых ключей нужно выбирать ТОЛЬКО из реально существующих в кабинете
   (список тебе даётся в контексте) — придумывать несуществующий ad_group_id
   нельзя, это приведёт к ошибке при записи.

5. Кампании бывают: TEXT_CAMPAIGN (ТГК, классическая текстово-графическая —
   поддерживает ручную стратегию ManualCpc с полным контролем
   ставок вручную), а также единые перформанс-кампании (ЕПК/UNIFIED_CAMPAIGN) и
   другие типы с автостратегиями (оплата за конверсии, за клики), где Bid
   недоступен для прямой записи (см. п.2). Для НОВЫХ кампаний в этом
   B2B-аккаунте с редкими конверсиями предпочтительна TEXT_CAMPAIGN на
   ManualCpc — она даёт предсказуемый контроль и не "наказывает" ставки
   ключей без быстрой статистики конверсий, в отличие от автостратегий ЕПК.

6. Кампания/группа объявлений проходят модерацию после создания — статус
   Status может быть DRAFT/MODERATION/ACCEPTED/REJECTED, State — ON/SUSPENDED.
   Активная кампания = State=ON И Status=ACCEPTED. Новая кампания не начнёт
   показываться немедленно — это нормально, а не ошибка apply.

7. AvgTrafficVolume (0–150) отражает объём СПРОСА в аукционе и не зависит от
   твоей ставки — если объём стабилен, а кликов/показов стало меньше, причина
   в ставке или позиции, а не в падении интереса рынка.
"""

# ─── Методология диагностики — сжатая версия PPC_Audit_Playbook, приложенного
#     пользователем (полная версия используется директологом для ручного
#     аудита). Задача — чтобы автономный ежедневный анализ рассуждал по тому
#     же дереву причин, а не только по видимым симптомам. ───────────────────

PPC_DIAGNOSTIC_METHODOLOGY = """
МЕТОДОЛОГИЯ ДИАГНОСТИКИ (сжатая версия аудиторского плейбука проекта) —
используй как дерево причин, а не только как список симптомов:

СТАТИСТИЧЕСКАЯ ЗНАЧИМОСТЬ (обязательно проверяй перед выводом):
  - Ключ: минимум 30 кликов, чтобы делать вывод о его CR
  - Группа: минимум 50 кликов
  - Кампания: минимум 100 кликов
  Ниже порога — НЕ утверждай наверняка, используй формулировку "недостаточно
  данных, требуется наблюдение" вместо категоричного вывода.

ПЯТЬ ТИПОВЫХ ПРИЧИН УХУДШЕНИЯ (проверяй в этом порядке):
  A. Снижение ставок — объём трафика (спрос) стабилен, но клики упали,
     ставка ниже предыдущего периода, позиция показа ухудшилась. Обычно
     вызвано автостратегией/ЕПК, которая снижает ставки ключей без
     конверсий за 2-4 недели. Решение: поднять ставку (если bid_editable),
     иначе — предложить перевод кампании на ручную стратегию (текстовое
     предложение, не автоприменяемое).
  B. Каннибализация — несколько кампаний/стратегий таргетируются на одну
     семантику одновременно → взаимно поднимают CPC на аукционе. Признак:
     синхронный рост CPC по всему направлению без роста конверсий.
  C. Замена рабочей кампании новой — кампания с историей конверсий исчезла,
     появилась похожая без истории и без конверсий.
  D. Информационный трафик в коммерческой группе — CR≈0% при 30+ кликах,
     в поисковых фразах нет коммерческих сигналов (купить/цена/поставка/
     конкретные размеры и марки). Решение: минус-слова или перевод на ОЗК.
  E. Несоответствие посадочной странице — трафик целевой, но CR упал без
     изменений в кампании; повод проверить страницу отдельно (вне
     возможностей автоматического apply — только текстовая рекомендация).

ПРИОРИТЕТ ЦЕЛЕВОЙ МЕТРИКИ: конечная цель — рост числа SQL (заявок, дошедших
минимум до КП) при разумной cost_per_sql_rub, а НЕ рост кликов/CTR самих по
себе. Ключ с отличным CTR, но без единого SQL при значимом объёме кликов —
это не успех, а потенциальный кандидат на паузу или минус-слова.

ЗОЛОТЫЕ ФРАЗЫ: реальные поисковые запросы с конкретными размерами/марками/
стандартами, которых ещё нет в семантике как отдельного точного ключа — их
стоит явно выделять в change_type=add_keywords, а не только чистить минус-словами.
"""


def get_llm_knowledge_block() -> str:
    """Собранный текст для дописывания в system prompt любого LLM-вызова."""
    return YANDEX_API_MECHANICS + "\n" + PPC_DIAGNOSTIC_METHODOLOGY
